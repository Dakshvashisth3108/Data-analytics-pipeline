# ---------------------------------------------------------------------------
# Streamlit-container dependency set.
#
# This is a deliberate SUBSET of the project's root requirements.txt — it
# contains only what the dashboard imports at runtime. The Spark pipeline
# packages (pyspark, delta-spark, confluent-kafka, faker) are excluded
# because the dashboard reads the pipeline's OUTPUT (parquet + Chroma
# index), never runs the pipeline itself. Dropping pyspark alone keeps the
# image ~400 MB smaller and the build minutes faster.
#
# Version pins are IDENTICAL to root requirements.txt — no drift.
# torch is installed separately in the Dockerfile (CPU-only wheel).
# ---------------------------------------------------------------------------

# Web UI
streamlit==1.39.0
plotly==5.24.1

# Data access (reads Gold parquet)
pandas==2.2.3
pyarrow>=18.0.0,<21.0.0

# NL->SQL engine
duckdb>=1.0.0,<2.0.0
sqlglot>=25.0.0,<27.0.0

# RAG: vector store + embedding model
chromadb>=0.5.0,<2.0.0
sentence-transformers>=3.0.0,<5.0.0

# LLM HTTP client (talks to Ollama) + config
requests>=2.32.0,<3.0.0
pyyaml==6.0.2
python-dotenv==1.0.1
