# PySpark 4.0+ ships with Hadoop 3.4+, which fixes the
# `Subject.getSubject is not supported` failure on Java 24+ that
# Spark 3.5.x trips on. Required for Java 26.
pyspark==4.0.0
delta-spark==4.0.0

# Kafka clients
kafka-python==2.0.2
confluent-kafka==2.5.0

# Config / data tooling
pyyaml==6.0.2
faker==30.3.0
pandas==2.2.3
pyarrow==17.0.0
python-dotenv==1.0.1
structlog==24.4.0

# Dashboard
streamlit==1.39.0
plotly==5.24.1

# Tests
pytest==8.3.3
pytest-cov==5.0.0
