# PySpark 4.x. Note: Spark 4.x bundles Hadoop 3.4.1 which still calls
# Subject.getSubject() in UserGroupInformation -- that method is
# permanently disabled in Java 24+. So Spark on Java 26 fails at
# startup. Use Java 21 LTS for the Spark jobs (see
# scripts/setup_java21.ps1 + scripts/spark-env.ps1). Once Spark
# bundles Hadoop 3.4.2+, this constraint will go away.
pyspark==4.1.1
delta-spark==4.0.0

# Kafka client (confluent-kafka is the one we use; 2.6+ ships Python 3.13 wheels).
confluent-kafka>=2.6.0,<3.0.0

# Config / data tooling
# pyarrow 18+ is the first version with Python 3.13 wheels — older ones fall
# back to a source build that fails on 3.13's setuptools changes.
pyyaml==6.0.2
faker==30.3.0
pandas==2.2.3
pyarrow>=18.0.0,<21.0.0
python-dotenv==1.0.1
structlog==24.4.0

# Dashboard
streamlit==1.39.0
plotly==5.24.1

# Tests
pytest==8.3.3
pytest-cov==5.0.0
